# Copy to .env for local overrides. All values are optional and have safe defaults.

# Runtime environment label surfaced in the UI.
VITE_APP_ENV=development

# Display name of the application.
VITE_APP_NAME=SyncBit

# Display version. When unset, the backend-provided version is used.
VITE_APP_VERSION=0.1.0